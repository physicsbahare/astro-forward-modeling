# Historical plan only: C5j failed locally; never dispatched to repeat the known failure.
name: gate-c-agn-fourier-controls
on:
  workflow_dispatch:
  push:
    branches: [verification-v0.1]
    paths:
      - scripts/run_agn_fourier_controls.py
      - tests/test_agn_fourier_controls.py
      - benchmarks/zhuang_shen_2024/C5J_PROTOCOL.md
      - .github/workflows/gate-c-agn-fourier-controls.yml
permissions:
  contents: read
  actions: read
concurrency:
  group: agn-fourier-controls-${{ github.ref }}
  cancel-in-progress: false
jobs:
  fourier-controls:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        host_n: [1, 4]
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements-agn-empirical-psf-phase.txt -e '.[test]'
      - run: python -m pytest -q tests/test_agn_fourier_controls.py tests/test_agn_compact_renderer.py tests/test_agn_empirical_psf_transfer.py
      - uses: actions/download-artifact@v4
        with:
          name: agn-compact-renderer-n${{ matrix.host_n }}
          run-id: 33766246396
          github-token: ${{ github.token }}
          path: parent-renderer
      - name: Frozen Fourier-control diagnosis
        run: python scripts/run_agn_fourier_controls.py --source parent-renderer --host-n ${{ matrix.host_n }} --out fourier-controls-output
      - if: always()
        run: |
          mkdir -p fourier-controls-output
          git rev-parse HEAD > fourier-controls-output/commit.txt
          python -m pip freeze > fourier-controls-output/pip-freeze.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agn-fourier-controls-n${{ matrix.host_n }}
          path: fourier-controls-output
