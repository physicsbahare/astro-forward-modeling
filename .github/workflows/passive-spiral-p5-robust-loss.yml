name: passive-spiral-p5-robust-loss

on:
  push:
    branches:
      - verification-v0.1
    paths:
      - "benchmarks/passive_spiral_redshift/P5_PROTOCOL.md"
      - "verification/passive_spiral_robust_loss.py"
      - "tests/test_passive_spiral_robust_loss.py"
      - "scripts/run_passive_spiral_robust_loss_p5.py"
      - ".github/workflows/passive-spiral-p5-robust-loss.yml"
      - "pyproject.toml"
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
    paths:
      - "benchmarks/passive_spiral_redshift/P5_PROTOCOL.md"
      - "verification/passive_spiral_robust_loss.py"
      - "tests/test_passive_spiral_robust_loss.py"
      - "scripts/run_passive_spiral_robust_loss_p5.py"
      - ".github/workflows/passive-spiral-p5-robust-loss.yml"
      - "pyproject.toml"
  workflow_dispatch:

jobs:
  p5-robust-loss:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install verification harness
        run: python -m pip install -e ".[test]"

      - name: Record software provenance
        run: python -m pip freeze | tee passive-spiral-p5-pip-freeze.txt

      - name: Run P5 regression tests
        run: pytest -q tests/test_passive_spiral_robust_loss.py

      - name: Download exact frozen Gate-D real cutout
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p real_artifact
          gh run download 33941326833 -n gate-d-cosmosweb-real-cutout -D real_artifact
          echo '764d542f2417810c904bce711455b3eb69c70cf388f97348cb857b056b2dd66d  real_artifact/cosmosweb_f444w_30mas_A1_ID4204_real_cutout.fits' | sha256sum --check --strict

      - name: Run frozen P5 robust-loss experiment
        run: |
          python scripts/run_passive_spiral_robust_loss_p5.py \
            --real-fits real_artifact/cosmosweb_f444w_30mas_A1_ID4204_real_cutout.fits \
            --matrix benchmarks/gate_d/COSMOSWEB_D1_INJECTION_MATRIX.json \
            --out benchmark_output/passive_spiral_redshift/p5_robust_loss \
            | tee passive-spiral-p5-stdout.txt

      - name: Upload P5 metrics and provenance
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: passive-spiral-p5-robust-loss
          path: |
            passive-spiral-p5-pip-freeze.txt
            passive-spiral-p5-stdout.txt
            benchmark_output/passive_spiral_redshift/p5_robust_loss/
          if-no-files-found: warn
