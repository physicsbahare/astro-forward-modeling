name: gate-c-agn-cross-sampling
on:
  push:
    branches: [verification-v0.1]
    paths:
      - scripts/run_agn_cross_sampling.py
      - tests/test_agn_cross_sampling.py
      - .github/workflows/gate-c-agn-cross-sampling.yml
  workflow_dispatch:
jobs:
  cross-sampling:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        host_n: [1, 4]
        fit_factor: [4, 8]
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[test]'
      - run: python -m pytest -q tests/test_agn_cross_sampling.py tests/test_agn_sampling.py tests/test_agn_nuclear_fraction_noiseless.py tests/test_agn_nuclear_fraction.py
      - name: Record provenance
        run: |
          mkdir -p benchmark_output/agn_cross_sampling
          git rev-parse HEAD > benchmark_output/agn_cross_sampling/commit.txt
          python -m pip freeze > benchmark_output/agn_cross_sampling/pip-freeze.txt
      - run: python scripts/run_agn_cross_sampling.py --host-n ${{ matrix.host_n }} --fit-factor ${{ matrix.fit_factor }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agn-cross-sampling-n${{ matrix.host_n }}-f${{ matrix.fit_factor }}
          path: benchmark_output/agn_cross_sampling/
          if-no-files-found: error
