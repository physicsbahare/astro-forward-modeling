name: gate-c-agn-noise-ensemble
on:
  push:
    branches: [verification-v0.1]
    paths:
      - scripts/run_agn_noise_ensemble.py
      - tests/test_agn_noise_ensemble.py
      - .github/workflows/gate-c-agn-noise-ensemble.yml
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  noise-ensemble:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        host_snr: [100, 20, 5]
        block: [0, 1, 2, 3]
    env:
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -e '.[test]'
      - run: python -m pytest -q tests/test_agn_noise_ensemble.py tests/test_agn_estimated_weights.py tests/test_agn_shot_noise.py tests/test_agn_nuclear_fraction_noiseless.py tests/test_agn_nuclear_fraction.py
      - uses: actions/download-artifact@v4
        with:
          name: agn-shot-noise-snr${{ matrix.host_snr }}
          run-id: 33680659156
          github-token: ${{ github.token }}
          path: source_noise
      - name: Record provenance
        run: |
          mkdir -p benchmark_output/agn_noise_ensemble
          git rev-parse HEAD > benchmark_output/agn_noise_ensemble/commit.txt
          python -m pip freeze > benchmark_output/agn_noise_ensemble/pip-freeze.txt
      - run: python scripts/run_agn_noise_ensemble.py --input source_noise --block ${{ matrix.block }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agn-noise-ensemble-snr${{ matrix.host_snr }}-block${{ matrix.block }}
          path: benchmark_output/agn_noise_ensemble/
          if-no-files-found: error
